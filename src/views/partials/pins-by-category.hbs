<div class="box box-link-hover-shadow">
  <p class="subtitle">Pins per category</p>
  <div class="columns">
    {{#each pinTotalCategory}}
      <div class="column">
        <b>{{_id}}</b>
        <p class="subtitle-2">{{count}}</p>
      </div>
    {{/each}}
  </div>
</div>